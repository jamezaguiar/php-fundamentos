<?php

$a = true;
$b = false;

var_dump($a && $b); // Também pode-se usar 'and'
var_dump($a || $b); // Também pode-se usar 'or'
var_dump($a xor $b); // Ou exclusivo
var_dump(!$a); // Também pode-se usar 'not'